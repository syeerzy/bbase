#!chezscheme

;;
;; 管茂东
;;
(library (string helpers)
  (export
    splite
    join)
  (import (chezscheme))
  
  ;;
  ;; 把str按照char分割
  ;;
  (define (splite sep str)
    ,,,)
  
  ;;
  ;; 把多个obj的字符串形式, 用sep串起来
  ;; 
  (define (join sep objs)
    ,,,))

